[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
# Suppress liboqs' startup banner so a failure is the only thing that stands out.
filterwarnings =
    ignore::DeprecationWarning
